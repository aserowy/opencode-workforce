#!/bin/sh
set -eu

root_dir="$(cd "$(dirname "$0")/.." && pwd)"
requirements_dir="$root_dir/requirements"

find "$requirements_dir" -type f -name "feature.md" \
  -exec grep -l "^- Status: plan$\|^- Status: execution$" {} \; \
  | sed "s#^$root_dir/##" \
  | sort
